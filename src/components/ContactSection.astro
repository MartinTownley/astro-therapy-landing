---
import Picture from 'astro/components/Picture.astro'
const { id, imageSrcPortrait, imageSrcLandscape, imageAlt = '' } = Astro.props
---

<section
  id={id}
  class="relative min-h-screen snap-start flex items-center justify-center bg-theme-pink/60 overflow-hidden"
>
  <!-- Background image -->
  <div class="absolute inset-0 z-0 overflow-hidden">
    <Picture
      src={imageSrcPortrait}
      formats={['avif', 'webp']}
      alt="A marbled image."
      loading="lazy"
      layout="full-width"
      class="contact-bg-image absolute -top-32 w-full h-[calc(100%+16rem)] object-cover block lg:hidden"
    />
    <Picture
      src={imageSrcLandscape}
      formats={['avif', 'webp']}
      alt="A marbled image."
      loading="lazy"
      layout="full-width"
      class="contact-bg-image absolute -top-32 w-full h-[calc(100%+16rem)] object-cover hidden lg:block"
    />
    <div class="absolute inset-0 bg-black/40"></div>
  </div>

  <!-- Foreground content -->
  <div class="relative z-10 w-full mx-auto px-0 md:max-w-lg lg:max-w-2xl">
    <slot />
  </div>
</section>
